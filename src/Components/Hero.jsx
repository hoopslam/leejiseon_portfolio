import React from 'react'
import hero from '../image/hero.gif'

function Hero() {
    return (
        <div className="hero">
            <img src={hero} alt="hero-img"/>
        </div>
    )
}

export default Hero
